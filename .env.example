# AWS credentials (optional if already authenticated via AWS CLI/IAM roles)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1

# IAM role for AgentCore execution (for sample agent deployment)
IAM_ROLE_ARN=arn:aws:iam::account:role/AgentCoreExecutionRole